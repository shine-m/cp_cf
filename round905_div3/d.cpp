#include<bits/stdc++.h>
using namespace std;
#define ln "\n" 
int main()
{
//ios_base::sync_with_stdio(0);cin.tie(0);cout.tie(0);
multiset<int> ms;


int t;
cin>>t;
int maxr = 0;
int minl = INT_MAX;
int maxr = 0;
while(t--){

    char c;
    int l, r;

    cin >> c >> l >> r;
    if(c=='+' && ms.begin()!=ms.end()){

if(l<minl){
    minl = l;
    
} 

    }



}  
 return 0;
}